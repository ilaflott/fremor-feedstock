schema_version: 1

context:
  python_min: "3.11"
  version: "0.9.6"

package:
  name: fremor
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/f/fremor/fremor-${{ version }}.tar.gz
  sha256: ddba600e9c1bf88d7e275f236536811c72391a0b7286c4a1d373220cc80f8ac4

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - fremor = fremor.cli:fremor

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=77.0.0
    - pip
  run:
    - python >=${{ python_min }}
    - click
    - cmor
    - netcdf4
    - numpy
    - pyyaml

tests:
  - python:
      imports:
        - fremor
      pip_check: true
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - fremor --help
      - fremor --version

about:
  summary: Model output rewriter (CMORizer) for FRE/FMS based models
  license: Apache-2.0
  license_file: LICENSE.md
  homepage: https://pypi.org/project/fremor/

extra:
  recipe-maintainers:
    - ilaflott
    - fmalatino
