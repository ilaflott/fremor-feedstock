schema_version: 1

context:
  python_min: "3.11"
  version: "0.9.5"

package:
  name: fremor
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/f/fremor/fremor-${{ version }}.tar.gz
  sha256: 23f1a3028bbc296f52f035789a4b82b1db1e734b778c580977e525ef811d3987

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - fremor = fremor.cli:fremor

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=77.0.0
    - pip
  run:
    - python >=${{ python_min }}
    - click
    - cmor
    - netcdf4
    - numpy
    - pyyaml

tests:
  - python:
      imports:
        - fremor
      pip_check: true
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - fremor --help
      - fremor --version

about:
  summary: Model output rewriter (CMORizer) for FRE/FMS based models
  license: Apache-2.0
  license_file: LICENSE.md
  homepage: https://pypi.org/project/fremor/

extra:
  recipe-maintainers:
    - ilaflott
    - fmalatino
